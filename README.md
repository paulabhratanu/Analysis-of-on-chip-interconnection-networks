# Analysis-of-on-chip-interconnection-network

In all the following folders there is a shell script (grid.sh).
Run->./grid.sh a b c d;where a and b = size of the matrix and c=start node,d=destination node.
